- **Actividad 2.1 — Depuración de código con errores (Herencia, polimorfismo, excepciones)**
- **(Markdown - Tabla de depuracion)**
#
| # | Línea | Error encontrado | Tipo (sintaxis/lógico) | Corrección |
| :--- | :--- | :--- | :--- | :--- |
| 1 |**Linea:** `def __init (self, codigo, tipo)` |**Error Encontrado:** Falta la doble raya al piso al final de la palabra **init** siempre requiere de la doble raya al piso se requiere antes y despues de la palabra **init** para que sea correcta la sintaxis del **init**. | **Error Tipo:** Sintaxis | `def __init__(self, codigo, tipo):` |
| 2 |**Linea:** `self.codigo codigo` |**Error Encontrado:** Falta el signo de igual **=** el cual al tratarse de una linea de **self** necesita el simbolo de igual **=** para funcionar ya que solo esta diciendo repetidamente la palabra codigo sin hacer nada debido a la ausencia del **=** siendo un error de sintaxis. | **Error Tipo:** Sintaxis | `self.codigo = codigo` |
| 3 |**Linea:** `def asignar(self, responsable)` |**Error Encontrado:** Al final de esta linea de codigo se omite el doble punto **:** el cual es vital para el funcionamiento de codigo del cual solo por la falta del doble punto al final termine catalogando esta linea como erronea por sintaxis |**Error Tipo:** Sintaxis | `def asignar(self, responsable):` |
| 4 |**Linea:** `def init (self, codigo, procesador):` |**Error Encontrado:** Al igual que el primer error listado este tambien se encuentra en la palabra **init** a causa de ausencia de las dobles raya al piso pero aca se trata de la ausencia de las rayas dobles antes y despues de la palabra **init** lo es un error de sintaxis.|**Error Tipo:** Sintaxis | `def __init__(self, codigo, procesador):` |
| 5 | **Linea:** `super(). init (codigo, "PC")` |**Error Encontrado:** Faltan las dobles raya piso sumados a que despues de la palabra **self.** hay  un espacio e cual no deberia presentarse causando error de sintaxis. | **Error Tipo:** Sintaxis | `super().__init__(codigo, "PC")` |
| 6 |**Linea:** `if self.estado = "disponible":` | **Error Encontrado:** La linea presente solo tiene uno de 2 signos de igual **=** del cual el segundo se requiere para la comparacion logica siendo un error de logica en el codigo a la vez que de sintaxis al ser por una omision al escribir.|**Error Tipo:** Lógico + Sintaxis | `if self.estado == "disponible":` |
| 7 | **Linea:** `def init (self, codigo, tipo impresion):` | **Error Encontrado:** Tambien se ausentan las dobles raya al piso antes y despues de la palabra **init* sumado a que en la seccion tipo_impresion se ausenta la linea al piso dejando el espacio en blanco siendo por ambos motivos un error de sintaxis:|**Error Tipo:** Sintaxis | `def __init__(self, codigo, tipo_impresion):` |